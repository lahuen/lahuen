name: Terraform
on:
  push:
    branches: ["main"]
    paths: ["iac/**"]

  pull_request:
    branches: ["main"]
    paths: ["iac/**"]
    
env:
  AWS_REGION : "eu-west-1"
  GHA_ROLE: ${{ secrets.GHA_ROLE }}

defaults:
  run:
    working-directory: iac

permissions:
  id-token: write
  contents: read    
  pull-requests: write
  
jobs:
  Terraform:
    runs-on: ubuntu-latest
    steps:
      - name: Git clone the repository
        uses: actions/checkout@v3
                  
      - name: configure aws credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ env.GHA_ROLE }}
          role-session-name: ghaSession
          aws-region: ${{ env.AWS_REGION }}
      
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v2
        with:
          terraform_version: 1.3.0  

      - name: Terraform Init
        id: init
        run: terraform init
          
      - name: Terraform fmt
        id: fmt
        run: terraform fmt -check
        continue-on-error: true
      
      - name: Terraform Validate
        id: validate
        run: terraform validate -no-color
        
      - name: Terraform Plan
        id: plan
        run: terraform plan -no-color -input=false -lock=false
        continue-on-error: true
        
      - name: Terraform Plan Status
        if: steps.plan.outcome == 'failure'
        run: exit 1

      - name: Terraform Apply
        id: apply
        run: terraform apply -no-color -input=false --auto-approve -lock=false
        if: steps.plan.outcome == 'success' && contains(github.event.comment.body, '/apply')
